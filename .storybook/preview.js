/** @type { import('@storybook/html').Preview } */
const preview = {
    parameters: {
        options: {
            storySort: {
                order: ['Molecules'],
            },
        },
    },
};

export default preview;
